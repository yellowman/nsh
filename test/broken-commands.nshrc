#This is a list of broken commands that to track what commands are still incomplete 
# or which the commands nsh functionality is not alligned with the latest OpenBSD developments.
show sadb
show eigrp
