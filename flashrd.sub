###
#
# fallback harness

c() {
 [ -z "$1" ] && exit 1
 lvl="$1"
 shift

 [ -z "$*" ] && exit 1

 if ! eval $*; then
  echo % $* failure
  case $lvl {
  4)
   4; 3; 2; 1; 0;
   ;;
  3)
   3; 2; 1; 0;
   ;;
  2)
   2; 1; 0;
   ;;
  1)
   1; 0;
   ;;
  0)
   0;
   ;;
  }
 fi
}
